services:
  - type: web
    name: lumen-ai
    env: node
    buildCommand: echo "No build needed"
    startCommand: node server.js
    envVars:
      - key: ANTHROPIC_API_KEY
        sync: false
      - key: PORT
        value: 3000
